{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "spago bundle-module -m Main -t output/Main/index.js -u '+RTS -N2 -RTS' && parcel build --public-url . -d ../docs index.html",
    "pscid:build": "spago build",
    "start": "parcel index.html"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
