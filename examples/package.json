{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "pulp build -- +RTS -N2 -RTS && parcel -d ../docs index.html",
    "postinstall": "bower install",
    "pscid:build": "pulp build",
    "start": "parcel index.html"
  },
  "devDependencies": {
    "parcel": "^1.12.3"
  }
}
